{
  "name": "web_worker_demo",
  "version": "1.0.0",
  "description": "A demo for web workers in JS",
  "main": "src/main.js",
  "scripts": {
    "dev": "browser-sync start --server --files 'index.html, src/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kyle Tolle",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.29.3"
  }
}
